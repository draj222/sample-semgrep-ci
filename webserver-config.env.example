# Webserver Configuration Example
# Copy this file to webserver-config.env and update with your actual values

# Internal webserver URL for posting scan results
WEBSERVER_URL=http://webserver.local/api/results

# API key for webserver authentication
# This should be a secure, randomly generated key
API_KEY=your-secure-api-key-here

# Optional: Custom webserver endpoint
# WEBSERVER_URL=https://your-internal-server.com/api/security-results

# Optional: Additional webserver headers (if needed)
# WEBSERVER_HEADERS={"X-Custom-Header": "value"}

# Optional: Webserver timeout settings (in seconds)
# WEBSERVER_TIMEOUT=30

# Optional: Retry settings for failed POST requests
# WEBSERVER_RETRY_ATTEMPTS=3
# WEBSERVER_RETRY_DELAY=5
